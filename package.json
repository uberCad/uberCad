{
  "name": "react-three-server",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:Dubiy/react-three-socket.git",
  "author": "Gary <netzver@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "start node index.js && cd client && npm run start",
    "heroku-postbuild": "cd client && npm run build",
    "build": "cd client && npm run build",
    "test": "standard --verbose && babel-node ./node_modules/.bin/tape ./test/**/*.js --require ignore-styles --require ./test/setup.js | faucet",
    "fix": "standard --verbose --fix | snazzy"
  },
  "dependencies": {
    "express": "^4.16.2",
    "faucet": "0.0.1",
    "npm-run-all": "^4.1.2",
    "reselect": "^3.0.1",
    "snazzy": "^7.1.1",
    "socket.io": "^2.0.4",
    "standard": "^11.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.15.1"
  }
}
