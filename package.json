{
  "name": "react-three-server",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:Dubiy/react-three-socket.git",
  "author": "Gary <netzver@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "start node index.js && cd client && yarn start",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build",
    "build": "cd client && react-scripts build",
    "test": "standard"
  },
  "dependencies": {
    "express": "^4.16.2",
    "reselect": "^3.0.1",
    "socket.io": "^2.0.4",
    "standard": "^11.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.15.1"
  }
}
